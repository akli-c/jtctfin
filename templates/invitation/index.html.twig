{% extends 'base.html.twig' %}

{% block body %}
    <h1>Set your password</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Set password</button>
    {{ form_end(form) }}
{% endblock %}
