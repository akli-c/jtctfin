{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block content %}
<div class="example-wrapper">
    <h1>Hello {{ app.user.firstname }} !</h1>
</div>
<table class="table table-striped table-light mt-5">
    <thead class="table-dark">
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Lastname</th>
            <th scope="col">Firstname</th>
            <th scope="col">Email Adress</th>
            <th scope="col">Phone Number</th>
        </tr>
    </thead>
    <tbody>
        {%for user in users%}
        <tr name="elements" id="{{ 'user'~ user.id}}">
            <th scope="row">{{ user.id }}</th>
            <td>{{ user.lastname }}</td>
            <td>{{ user.firstname }}</td>
            <td>{{ user.email }}</td>
            <td>{{ user.phone }}</td>
            <td><a href=""admin/invite/{id}"">Invite</a></td>
            {#<td><a href="{{ asset('/Uploads/Images/' ~ admin.photo) }}">{{ admin.photo }}</a></td>#}
        </tr>
{%endfor%}
<tfoot>
    <tr>
        <td></td>

        <td></td>
        <td></td>
        <td></td>
        <td></td>
        <td></td>
        <td><a href="/admin/add">Add a new user </a></td>
    </tr>
</tfoot>
    </tbody>
</table>

{% endblock %}
